"""AUTOEDITING FORGE — free, headless multi-camera podcast auto-editor.

Turns separately-recorded camera angles + isolated per-person mics into a
finished long-form episode (with automatic speaker-based camera switching),
plus vertical shorts with burned-in captions.

No paid software required: it drives ffmpeg + (optional) faster-whisper.
"""

__version__ = "0.1.0"
