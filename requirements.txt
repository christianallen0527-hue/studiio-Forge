numpy>=1.24
PyYAML>=6.0

# Optional — only needed for transcripts, captions, and shorts selection.
# Install separately if you want them:
#   pip install faster-whisper
Flask>=3.0   # only for the local UI (ui/server.py)

# Optional — public YouTube study for the editorial learning loop:
#   pip install yt-dlp
#   python -m autoedit.learning study "https://www.youtube.com/watch?v=..."
